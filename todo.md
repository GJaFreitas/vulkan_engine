[] Query performance counter;
[] move the file reading function from vulkan to the base layer;
[] have a file with all the global variables and be able to modify it on the fly with hot reloading kind of thing;
[?] create logging functionality;

